_target_: cardiac_latent_ode.trainer.trainer.Trainer

checkpoint_dir: ${paths.output_dir}/checkpoints
max_epochs: 150
grad_clip_norm: 1.0
progress_bar_update_interval: 10
max_generation_samples: 1000
subsample_generation_frames: 5
